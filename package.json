{
  "devDependencies": {
    "@openzeppelin/contracts": "^3.2.0",
    "ethereum-waffle": "^3.1.1",
    "ethers": "^5.0.17",
    "rimraf": "^3.0.2",
    "solc": "0.6.12"
  },
  "scripts": {
    "build": "rimraf ./build/*.json && waffle"
  }
}
